<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="mysql">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:minWidth">147px</item>
        <item name="android:minHeight">55px</item>
        <item name="android:paddingLeft">32px</item>
        <item name="android:paddingRight">32px</item>
        <item name="android:paddingTop">2px</item>
        <item name="android:paddingBottom">2px</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">34.61px</item>
        <item name="android:background">@drawable/text</item>
    </style>
    <style name="MyDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/colorWhite</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>
